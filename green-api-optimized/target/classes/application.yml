server:
  port: 8081
  compression:
    enabled: true
    mime-types: application/json,text/plain,application/javascript
    min-response-size: 1024
management:
  endpoints:
    web:
      exposure:
        include: health,metrics
logging:
  level:
    com.example.optimized: INFO
